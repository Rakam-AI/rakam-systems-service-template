# Custom Rakam System

#### Start with "bash scripts/build_server.sh server_group_name"
Check system_config.yaml to see the server groups we set.
It's a simple automation of creating a custom rakam system template consisted of specific compononents we set in the server group.